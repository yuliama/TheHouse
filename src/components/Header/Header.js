import { Form, Button, Alert } from 'react-bootstrap';

export default function Header(){
    return(
        <div>
            <img className="logo"></img>
            <Button>Login</Button>
        </div>
    )
}